

beforeAll(() => {
    // connect to mongodb
    // seed
})

afterAll(() => {
    
})

describe("authenication", () => {
    it("should deny access to unauthenicated request", () => {
        expect(true).toBeTruthy()
    })
})