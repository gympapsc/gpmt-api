apiVersion: v1
kind: Service
metadata:
    name: gpmt-api
    namespace: "default"
spec:
    type: ClusterIP
    ports:
    - port: 80
    selector:
        app: gpmt-api
---
apiVersion: v1
kind: Service
metadata:
    name: gpmt-mongodb
    labels:
        app: gpmt-mongodb
spec:
    clusterIP: None
    ports:
    - port: 27017
      name: mongodb
    selector:
        app: gpmt-mongodb
