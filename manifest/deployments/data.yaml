apiVersion: apps/v1
kind: Deployment
metadata:
    name: gpmt-api-data
    namespace: api
spec:
    selector:
        matchLabels:
            app: gpmt-api-data
    template:
        metadata:
            labels:
                app: gpmt-api-data
        spec:
            containers:
            - name: gpmt-api-data
              image: gpmtregistry.azurecr.io/gpmt-api-data:v1
              ports:
              - containerPort: 80
            imagePullSecrets:
            - name: registry-token
